<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title><%= title %></title>
    <!-- <link rel="stylesheet" href="/styles.css">  Add your custom styles here -->
</head>
<body class="flex justify-center items-center p-6">
    <div class="login-container p-5 bg-gray-300 shadow-lg">
        <h1 class="bg-brown text-center text-black-700 font-bold">Login</h1>

        <!-- Login Form -->
        <form action="/api/v1/user/login" method="POST" class="p-5 space-y-3 space-x-3 font-semibold text-center">
            <!-- Email or Username -->
            <label for="usernameOrEmail">Username or Email:</label>
            <input type="text" id="usernameOrEmail" name="username" placeholder="Enter your username or email" required />

            <!-- Password Input -->
            <label for="password">Password:</label>
            <input type="password" id="password" name="password" placeholder="Enter your password" required />
            
            <br>
            <!-- Submit Button -->
            <button type="submit" class="bg-blue-700 text-white p-1 rounded px-3">Login</button>
        </form>

        <p class="text-center">Don't have an account? <a href="api/v1/user/register" class="text-blue-700 font-semibold">Register Here</a></p> <!-- Link to registration page -->

        <% if (message) { %>
            <div class="error-message" style="display:none">
                <p><%= message %></p>
            </div>
        <% } %>
    </div>
</body>
</html>
